//
//  SRCutomButton.swift

//
//  Created by SAS 
//  Copyright © 2018 MAC7. All rights reserved.
//

import Foundation
import UIKit

class SRCutomButton: UIButton {
    required init?(coder aDecoder: NSCoder) {
        super.init(coder: aDecoder)
        /*
        self.layer.cornerRadius = 5.0
        self.layer.borderColor = Global.kAppColor.DarkGray.cgColor
        self.titleLabel?.font = UIFont.init(name: Global.kFont.Helvetica_Regular, size: Global.kFontSize.TextFieldSmallSize_8)
        if self.tag == 101{
            self.layer.borderWidth = 1.0
            self.titleLabel?.textColor = Global.kAppColor.DarkGray
            self.backgroundColor = Global.kAppColor.WhiteColor
        }
        else if self.tag == 102{
            self.layer.borderWidth = 0.0
            self.titleLabel?.textColor = Global.kAppColor.WhiteColor
            self.backgroundColor = Global.kAppColor.ButtonColor
        }
        else if self.tag == 103{
            self.layer.cornerRadius = 0.0
            self.layer.borderWidth = 0.0
            self.titleLabel?.textColor = Global.kAppColor.WhiteColor
            self.backgroundColor = Global.kAppColor.ButtonColor
        }
        else{
            self.layer.borderWidth = 0.0
            self.titleLabel?.textColor = Global.kAppColor.DarkGray
            self.backgroundColor = UIColor.clear
        }*/
    }
}

